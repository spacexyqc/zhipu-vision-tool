# 智谱 AI 官方 SDK（图像理解）
zai-sdk>=0.0.3.3

# GUI 界面库（Python 内置，但明确列出更专业）
tk

# 图像处理（可选，用于更复杂的图像操作）
Pillow>=9.0.0

# 拖拽支持（可选增强功能）
python-tk-dnd2 ; sys_platform == "win32" or sys_platform == "linux"

# 其他通用依赖（如果你未来扩展功能）
requests
